import React from 'react';
import logo from '../../logo.png';
import './Header.css';

const Header = (props) => {
    return(
        <div className="Header">
           <h1>Inside Moves </h1>
        </div>
    );
}

export default Header;